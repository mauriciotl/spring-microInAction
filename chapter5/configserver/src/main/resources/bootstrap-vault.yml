spring:
  profiles:
    active: vault
  cloud:
    config:
      server:
        vault:
          host: host.docker.internal
          port: 8200
          scheme: http
          kvVersion: 2
          authentication: TOKEN
          token: root
          #Following are for the example property from the book.
          backend: licensing-service
          default-key: default